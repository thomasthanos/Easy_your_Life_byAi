<Window x:Class="MyApp.info"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Info" Height="450" Width="700"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <!-- Electric Blue Color Palette -->
        <Color x:Key="AccentColor">#FF00B4FF</Color>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <Color x:Key="DarkBackground">#FF121212</Color>
        <SolidColorBrush x:Key="DarkBackgroundBrush" Color="{StaticResource DarkBackground}"/>

        <!-- Glow Effect -->
        <DropShadowEffect x:Key="ButtonGlow" Color="#00B4FF" BlurRadius="20" Opacity="0" Direction="0"/>
        <DropShadowEffect x:Key="SectionGlow" Color="#00B4FF" BlurRadius="15" Opacity="0.2" Direction="0"/>

        <!-- Window Control Buttons -->
        <Style x:Key="WindowButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#606060"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Background" Value="#2000B4FF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#4000B4FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Minimal Modern Scrollbar Style -->
        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid>
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Style>
                                            <Style TargetType="Thumb">
                                                <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                                                <Setter Property="Opacity" Value="0.5"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Thumb">
                                                            <Border Background="{TemplateBinding Background}"
                                                                    CornerRadius="4"
                                                                    Width="6"/>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Opacity" Value="0.8"/>
                                                    </Trigger>
                                                    <Trigger Property="IsDragging" Value="True">
                                                        <Setter Property="Opacity" Value="1.0"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Thumb.Style>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Section Gradient Background -->
        <LinearGradientBrush x:Key="SectionBackgroundBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FF1E1E1E" Offset="0.0"/>
            <GradientStop Color="#FF252526" Offset="1.0"/>
        </LinearGradientBrush>
    </Window.Resources>

    <Grid>
        <!-- Main Container -->
        <Border Background="{StaticResource DarkBackgroundBrush}" CornerRadius="10" Margin="20"
                BorderBrush="{StaticResource AccentBrush}" BorderThickness="3.5">
            <Border.Effect>
                <DropShadowEffect Color="#000000" BlurRadius="15" Opacity="0.3"/>
            </Border.Effect>

            <Grid>
                <!-- Title Bar -->
                <Border Background="#FF252526" Height="40" VerticalAlignment="Top" CornerRadius="10 10 0 0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Back Button -->
                        <Button Grid.Column="1" Style="{StaticResource WindowButtonStyle}" Width="30" Height="30" Content="←" Click="BackButton_Click" Margin="0,0,5,0"/>

                        <!-- Minimize Button -->
                        <Button Grid.Column="2" Style="{StaticResource WindowButtonStyle}" Width="30" Height="30" Content="─" Click="MinimizeButton_Click" Margin="0,0,5,0"/>

                        <!-- Close Button -->
                        <Button Grid.Column="3" Style="{StaticResource WindowButtonStyle}" Width="30" Height="30" Content="✕" Click="CloseButton_Click" Margin="0,0,10,0"/>
                    </Grid>
                </Border>

                <!-- Content Area with Custom Scrollbar -->
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="10,50,10,10">
                    <StackPanel>
                        <!-- Section 1: System Optimization -->
                        <Border Background="{StaticResource SectionBackgroundBrush}" CornerRadius="8" Margin="0,0,0,10" BorderBrush="#3000B4FF" BorderThickness="1">
                            <Border.Effect>
                                <DropShadowEffect Color="#00B4FF" BlurRadius="10" Opacity="0.2"/>
                            </Border.Effect>
                            <StackPanel Margin="15">
                                <TextBlock Text="System Optimization" Foreground="{StaticResource AccentBrush}" FontSize="18" FontWeight="Bold" RenderOptions.ClearTypeHint="Enabled" TextOptions.TextFormattingMode="Display"/>
                                <TextBlock Text="Automatically clean up temporary files, optimize system performance, and check system health." Foreground="White" FontSize="14" TextWrapping="Wrap" Margin="0,5,0,0" RenderOptions.ClearTypeHint="Enabled" TextOptions.TextFormattingMode="Display"/>
                                <Button Content="Optimize Now" Background="{StaticResource AccentBrush}" Foreground="White" FontSize="14" Margin="0,10,0,0" Padding="10,5" HorizontalAlignment="Left" BorderThickness="0" Cursor="Hand">
                                    <Button.Effect>
                                        <DropShadowEffect Color="#00B4FF" BlurRadius="5" Opacity="0.3"/>
                                    </Button.Effect>
                                </Button>
                            </StackPanel>
                        </Border>

                        <!-- Section 2: Software Management -->
                        <Border Background="{StaticResource SectionBackgroundBrush}" CornerRadius="8" Margin="0,0,0,10" BorderBrush="#3000B4FF" BorderThickness="1">
                            <Border.Effect>
                                <DropShadowEffect Color="#00B4FF" BlurRadius="10" Opacity="0.2"/>
                            </Border.Effect>
                            <StackPanel Margin="15">
                                <TextBlock Text="Software Management" Foreground="{StaticResource AccentBrush}" FontSize="18" FontWeight="Bold" RenderOptions.ClearTypeHint="Enabled" TextOptions.TextFormattingMode="Display"/>
                                <TextBlock Text="Install and manage popular applications with just one click. Keep your software up to date automatically." Foreground="White" FontSize="14" TextWrapping="Wrap" Margin="0,5,0,0" RenderOptions.ClearTypeHint="Enabled" TextOptions.TextFormattingMode="Display"/>
                                <Button Content="Manage Software" Background="{StaticResource AccentBrush}" Foreground="White" FontSize="14" Margin="0,10,0,0" Padding="10,5" HorizontalAlignment="Left" BorderThickness="0" Cursor="Hand">
                                    <Button.Effect>
                                        <DropShadowEffect Color="#00B4FF" BlurRadius="5" Opacity="0.3"/>
                                    </Button.Effect>
                                </Button>
                            </StackPanel>
                        </Border>

                        <!-- Section 3: Gaming & Entertainment -->
                        <Border Background="{StaticResource SectionBackgroundBrush}" CornerRadius="8" Margin="0,0,0,10" BorderBrush="#3000B4FF" BorderThickness="1">
                            <Border.Effect>
                                <DropShadowEffect Color="#00B4FF" BlurRadius="10" Opacity="0.2"/>
                            </Border.Effect>
                            <StackPanel Margin="15">
                                <TextBlock Text="Gaming  Entertainment" Foreground="{StaticResource AccentBrush}" FontSize="18" FontWeight="Bold" RenderOptions.ClearTypeHint="Enabled" TextOptions.TextFormattingMode="Display"/>
                                <TextBlock Text="Access platforms for downloading games and entertainment software. Activate and manage your gaming software." Foreground="White" FontSize="14" TextWrapping="Wrap" Margin="0,5,0,0" RenderOptions.ClearTypeHint="Enabled" TextOptions.TextFormattingMode="Display"/>
                                <Button Content="Explore Games" Background="{StaticResource AccentBrush}" Foreground="White" FontSize="14" Margin="0,10,0,0" Padding="10,5" HorizontalAlignment="Left" BorderThickness="0" Cursor="Hand">
                                    <Button.Effect>
                                        <DropShadowEffect Color="#00B4FF" BlurRadius="5" Opacity="0.3"/>
                                    </Button.Effect>
                                </Button>
                            </StackPanel>
                        </Border>

                        <!-- Section 4: System Settings -->
                        <Border Background="{StaticResource SectionBackgroundBrush}" CornerRadius="8" Margin="0,0,0,10" BorderBrush="#3000B4FF" BorderThickness="1">
                            <Border.Effect>
                                <DropShadowEffect Color="#00B4FF" BlurRadius="10" Opacity="0.2"/>
                            </Border.Effect>
                            <StackPanel Margin="15">
                                <TextBlock Text="System Settings" Foreground="{StaticResource AccentBrush}" FontSize="18" FontWeight="Bold" RenderOptions.ClearTypeHint="Enabled" TextOptions.TextFormattingMode="Display"/>
                                <TextBlock Text="Configure system settings, enable automatic login, and customize your experience." Foreground="White" FontSize="14" TextWrapping="Wrap" Margin="0,5,0,0" RenderOptions.ClearTypeHint="Enabled" TextOptions.TextFormattingMode="Display"/>
                                <Button Content="Configure Settings" Background="{StaticResource AccentBrush}" Foreground="White" FontSize="14" Margin="0,10,0,0" Padding="10,5" HorizontalAlignment="Left" BorderThickness="0" Cursor="Hand">
                                    <Button.Effect>
                                        <DropShadowEffect Color="#00B4FF" BlurRadius="5" Opacity="0.3"/>
                                    </Button.Effect>
                                </Button>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</Window>